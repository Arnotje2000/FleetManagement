1. cd naar folder location

2. Laad de 2 tar folders in docker in cmd:

	docker load -i frontend_image.tar
	docker load -i backend_image.tar

3. docker-compose up -d in cmd

3. Frontend openen in http://localhost:3000

4. database beschikbaar op http://localhost:8080